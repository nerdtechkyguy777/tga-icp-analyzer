# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — never commit secrets
.env
.env.*
!.env.example

# runtime data (local dev only — production uses Netlify Blobs)
/data/analyses.json

# keep ICP seed files in git for first Netlify deploy
!/data/icp-current.json
!/data/icp-history.json

# typescript
*.tsbuildinfo
next-env.d.ts
